export const Button = ({children}) => {
    return (
        <button className="font-medium px-3 py-2.5 mr-2 mb-2 rounded-lg w-full text-xl bg-gradient-to-r from-indigo-500 from-10% via-sky-500 via-30% to-emerald-500 to-90%">{children}</button>
    )
}
